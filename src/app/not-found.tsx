import React from "react";
export const dynamic = "force-dynamic";
const NotFound = () => {
  return (
    <div className="flex h-screen items-center justify-center">NotFound</div>
  );
};

export default NotFound;
